version: 0.2

phases:
    install:
        runtime-versions:
            dotnet: 3.1
    build:
        commands:
            - dotnet build -c Release ./awsbeanstactest/awsbeanstactest.csproj
            - dotnet test -c Release ./awsbeanstactest/awsbeanstactest.csproj
